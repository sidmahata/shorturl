{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FabsteiShorturl</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Token</th>
                <td>{{ entity.token }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ entity.url }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('redirect') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('redirect_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
